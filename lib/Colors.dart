import 'package:flutter/material.dart';

class AppColors {
  // Primary color - A deep, professional blue
  static const Color primaryColor = Color.fromARGB(255, 44, 62, 80);

  // Secondary color - A vibrant, energetic teal
  static const Color secondaryColor = Color(0xFF1ABC9C);

  // Accent color - A warm, friendly orange
  static const Color accentColor = Color(0xFFE67E22);

  // Background color - A soft, neutral grey
  static const Color backgroundColor = Color(0xFFF5F6FA);

  // Additional utility colors for text and components
  static const Color textPrimary = Color(0xFF2C3E50);
  static const Color textSecondary = Color(0xFF7F8C8D);
  static const Color dividerColor = Color(0xFFECF0F1);
  static const Color successColor = Color(0xFF2ECC71);
  static const Color errorColor = Color(0xFFE74C3C);
}
